from math import pi

#instead of put pi value import pi from math lib:

radius = float(input ("Input the radius of the circle : "))

print ("The area of the circle with radius " ,(radius), " is: " ,(pi * radius**2))
